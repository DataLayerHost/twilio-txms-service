{
  "name": "txms-service",
  "version": "1.0.1",
  "description": "Twilio TxMS service.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run start",
    "new": "twilio-run new",
    "deploy": "twilio-run deploy --override-existing-project",
    "promote": "twilio-run promote --from=dev --to=stage",
    "logs": "twilio-run logs --tail",
    "ui-active": "twilio api:serverless:v1:services:update --ui-editable"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.3.0",
    "axios": "^1.4.0",
    "twilio": "^4.14",
    "txms.js": "^1.0.3"
  },
  "devDependencies": {
    "twilio-run": "^3.5.3"
  },
  "engines": {
    "node": "14"
  },
  "author": "DataLayer",
  "license": "CORE",
  "repository": "https://github.com/DataLayerHost/twilio-txms-service",
  "homepage": "https://github.com/DataLayerHost/twilio-txms-service#readme",
  "bugs": {
    "url": "https://github.com/DataLayerHost/twilio-txms-service/issues"
  }
}
